{
  "name": "WebClock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run sync-env && python3 -m http.server ${PORT:-3000}",
    "build": "npm run sync-env && node scripts/build-static.mjs",
    "sync-env": "node scripts/generate-runtime-config.mjs",
    "prepare": "npm run sync-env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true
}
